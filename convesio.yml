import:
  - convesio/wordpress:latest
  # - origin: convesio/redis-cluster:4.0
  #   deligates:
  #     redis-name-change: change_redis
  #     redis-block: siltuss

#  - file::extensions/bot

services:
  wordpress:
    environment:
      WORDPRESS_DB_HOST : "0.0.0.0:3308"
      WORDPRESS_DB_NAME : "wordpress"
      WORDPRESS_DB_USER : "sumanTest"
      WORDPRESS_DB_PASSWORD : "sumanTest"
  mysql:
    environment:
      MYSQL_DATABASE : "wordpress"
      MYSQL_USER : "sumanTest"
      MYSQL_PASSWORD : "sumanTest"
